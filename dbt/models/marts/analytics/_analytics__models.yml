version: 2

models:
  - name: obt_games_stats_filtered
    description: "Filtered one-big-table of game statistics used for downstream analytics and dashboarding."
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - uuid
            - username_global
