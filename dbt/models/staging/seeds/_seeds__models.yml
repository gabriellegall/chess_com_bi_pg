version: 2

models:
  - name: stg_seeds__username_mapping
    description: "Standardizes the maintained username mapping, ensuring identity consolidation can be applied consistently across models."
    columns:
      - name: username
        description: "{{ doc('username') }}"
      - name: username_normalized
        description: "{{ doc('username_normalized') }}"
      - name: username_global
        description: "{{ doc('username_global') }}"